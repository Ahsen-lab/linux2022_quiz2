#ifndef QUIZ2_TEST1_H
#define QUIZ2_TEST1_H

#include <stdint.h>

/* test 1. */
uint32_t average1(uint32_t a1, uint32_t b1);
uint32_t average2(uint32_t a2, uint32_t b2);

/* test 2. */
uint32_t max(uint32_t a, uint32_t b);

/* test 3. */
uint64_t gcd64(uint64_t u, uint64_t v);
#endif
